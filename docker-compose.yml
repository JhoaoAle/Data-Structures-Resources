services:
  class-container:
    build:
      context: .
      dockerfile: Dockerfile
    image: arch-cpp-xeus
    ports:
      - "8888:8888"
    volumes:
      - .:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true